<% flash.each do |key, message| %>
  <% if key == 'alert' %>
    <% key  = 'primary' %>
  <% end %>

  <%= content_tag :p, message.html_safe, {class: "flash alert alert-#{key}", role: 'alert'} %>
<% end %>

<p><%= t('devise.confirmation_instructions.user', user: @email ) %>!</p>

<p><%=t('devise.confirmation_instructions.text' )%> below:</p>

<p><%= link_to t('devise.links.confirm'), confirmation_url(@resource, confirmation_token: @token) %></p>

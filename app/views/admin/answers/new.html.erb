<div class="row justify-content-center mt-3">
  <h1 class="text-center mb-3"><%= t('answers.new.title') %></h1>

  <h4 class="text-center mb-3">
    <%= @question.body %>
  </h4>

  <div class="card border-primary col col-md-4 p-3">
    <%= render 'form' %>
    <%= link_to t('links.back'), admin_question_path(@question) %>
  </div>
</div>

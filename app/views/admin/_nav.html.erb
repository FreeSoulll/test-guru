<div class="nav nav-admin">
  <span class="nav-admin title"><%= t('layouts.head.admin_panel') %> <%= t('layouts.head.title') %></span>

  <div class="nav-admin user">
    <%= t('layouts.body.user', email: current_user.email).html_safe %>
    <%= link_to t('links.exit'), destroy_user_session_path, method: :delete %>
  </div>
</div>

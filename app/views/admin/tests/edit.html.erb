<div class="row justify-content-center mt-3">
  <h1 class="text-center mb-3"><%= t('tests.edit.title', title: @test.title) %></h1>

  <div class="card border-primary col col-md-4 p-3">
    <%= render 'form' %>
    <%= link_to t('links.back'), admin_tests_path %>
  </div>
</div>

<div class="row justify-content-center mt-3">
  <h1 class="text-center mb-3"><%= t('badges.edit.title') %></h1>

  <div class="card border-primary col col-md-4 p-3">
    <%= render 'form' %>
    <div class="d-flex gap-2">
      <%= link_to t('links.back'), admin_badges_path %>
    </div>
  </div>
</div>
